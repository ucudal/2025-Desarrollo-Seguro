# Intento 1 (antes del fix).
curl -i -X POST http://localhost:5000/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "ejemplo1",
    "password": "ejemplo123",
    "email": "ejemplo1@gmail.com",
    "first_name": "<%= JSON.stringify(process.env) %>",
    "last_name": "<%= process.cwd() %>"
  }'

# Intento 2 (después del fix).
curl -i -X POST http://localhost:5000/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "test1",
    "password": "test123",
    "email": "test1@gmail.com",
    "first_name": "<%= JSON.stringify(process.env) %>",
    "last_name": "<%= process.cwd() %>"
  }'

# Intento 3 (después del fix).
curl -i -X POST http://localhost:5000/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "tester123",
    "password": "test123",
    "email": "tester@gmail.com",
    "first_name": "tester",
    "last_name": "testing"
  }'

# Intento de login.
curl -X POST http://localhost:5000/auth/login \
-H "Content-Type: application/json" \
-d '{
  "username": "tester123",
  "password": "test123"
}'